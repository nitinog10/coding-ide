# nitinog10/coding-ide — Documentation

> Auto-generated by [DocuVerse](https://docuverse.ai)

## Overview

The `repo_8b80424f4004` repository contains the source code for an AI-powered coding IDE platform. This platform allows users to write, execute, and collaborate on code in multiple programming languages, with integrated AI assistance for coding tasks. The project is structured into two main components: `backend` and `frontend`.

The `backend` directory houses the server-side logic, built with TypeScript and Node.js. It includes configuration files, Dockerfiles for various programming languages, and scripts for building Docker images and initializing the database. The backend communicates with AWS DynamoDB for data storage and provides RESTful API endpoints for user authentication, project management, and AI-assisted coding features.

The `frontend` directory contains the client-side application, built with React, TypeScript, and Tailwind CSS. It includes components for the AI assist panel, code editor, and project management, as well as context providers for authentication and editor state. The frontend communicates with the backend via RESTful API calls to enable features like code execution, AI assistance, and user authentication.

The project uses Docker for containerization, with a `docker-compose.yml` file to define and run multi-container applications. PowerShell scripts `start-backend.ps1` and `start-frontend.ps1` automate the setup and startup processes for the backend and frontend environments, respectively.

This AI-powered coding IDE platform is intended for developers, educators, and students who want an integrated environment for writing, executing, and collaborating on code with AI assistance.

## Architecture

```markdown
### Code Organization

The codebase is structured into two main directories: `backend` and `frontend`. Each directory contains its own set of configurations, scripts, and source code.

#### Backend

The `backend` directory is organized as follows:

- **`docker-images/`**: Contains Dockerfiles for different programming languages (C++, Java, JavaScript, Python) used for code execution.
- **`scripts/`**: Includes scripts for building Docker images and initializing the database.
- **`src/`**: The main source code directory, further divided into:
  - **`config/`**: Configuration files, such as DynamoDB setup.
  - **`middleware/`**: Middleware functions like authentication, error handling, and request logging.
  - **`routes/`**: Route definitions for different functionalities (AI, auth, code, projects, user).
  - **`services/`**: Service classes for AI interactions, database operations, and code execution.
  - **`types/`**: TypeScript type definitions.
  - **`utils/`**: Utility functions for authentication, gamification, and logging.

#### Frontend

The `frontend` directory is structured as follows:

- **`src/`**: The main source code directory, further divided into:
  - **`components/`**: Reusable UI components like `AIAssistPanel`, `CodeEditor`, and `Header`.
  - **`contexts/`**: Context providers for authentication and editor state.
  - **`pages/`**: Page components like `Editor`, `Login`, and `Register`.
  - **`services/`**: Service functions for API interactions.
  - **`types/`**: TypeScript type definitions.

### Data Flow

1. **User Interaction**: Users interact with the frontend, which sends requests to the backend.
2. **Routing**: Requests are routed to appropriate handlers in the `routes` directory.
3. **Middleware**: Middleware functions (e.g., authentication, error handling) process the requests.
4. **Service Layer**: The service layer (`aiService`, `database`, `executionService`) handles business logic and interacts with external services or databases.
5. **Response**: The response is sent back to the frontend, which updates the UI accordingly.

### Key Design Patterns

- **MVC (Model-View-Controller)**: The backend follows an MVC pattern, with routes acting as controllers, services as models, and the frontend as the view.
- **Middleware**: Middleware functions are used for cross-cutting concerns like authentication and logging.
- **Service Layer**: A dedicated service layer abstracts business logic from route handlers.

### Main Entry Points

- **Backend**: The main entry point is `backend/src/server.ts`, which sets up the Express server and routes.
- **Frontend**: The main entry point is `frontend/src/main.tsx`, which bootstraps the React application.
```

## Folder Structure

```
├── QUICKSTART.md
├── README.md
├── SETUP.md
├── docker-compose.yml
├── start-backend.ps1
├── start-frontend.ps1
├── .kiro/
│   └── specs/
│       └── ai-powered-coding-ide/
│           ├── .config.kiro
│           ├── requirements.md
│           └── tasks.md
├── backend/
│   ├── .dockerignore
│   ├── .env.example
│   ├── Dockerfile
│   ├── package.json
│   ├── tsconfig.json
│   ├── docker-images/
│   │   ├── cpp/
│   │   │   └── Dockerfile
│   │   ├── java/
│   │   │   └── Dockerfile
│   │   ├── javascript/
│   │   │   └── Dockerfile
│   │   └── python/
│   │       └── Dockerfile
│   ├── scripts/
│   │   ├── build-images.ps1
│   │   ├── build-images.sh
│   │   └── init-db.ts
│   └── src/
│       ├── server.ts
│       ├── config/
│       │   └── dynamodb.ts
│       ├── middleware/
│       │   ├── auth.ts
│       │   ├── errorHandler.ts
│       │   └── requestLogger.ts
│       ├── routes/
│       │   ├── ai.ts
│       │   ├── auth.ts
│       │   ├── code.ts
│       │   ├── projects.ts
│       │   └── user.ts
│       ├── services/
│       │   ├── aiService.ts
│       │   ├── database.ts
│       │   └── executionService.ts
│       ├── types/
│       │   └── index.ts
│       └── utils/
│           ├── auth.ts
│           ├── gamification.ts
│           └── logger.ts
└── frontend/
    ├── .dockerignore
    ├── .env.example
    ├── Dockerfile
    ├── index.html
    ├── package.json
    ├── postcss.config.js
    ├── tailwind.config.js
    ├── tsconfig.json
    ├── tsconfig.node.json
    ├── vite.config.ts
    └── src/
        ├── App.tsx
        ├── index.css
        ├── main.tsx
        ├── components/
        │   ├── AIAssistPanel.tsx
        │   ├── CodeEditor.tsx
        │   ├── Header.tsx
        │   ├── LanguageSelector.tsx
        │   ├── OutputConsole.tsx
        │   ├── ProjectsPanel.tsx
        │   └── ProtectedRoute.tsx
        ├── contexts/
        │   ├── AuthContext.tsx
        │   └── EditorContext.tsx
        ├── pages/
        │   ├── Editor.tsx
        │   ├── Login.tsx
        │   └── Register.tsx
        ├── services/
        │   └── api.ts
        └── types/
            └── index.ts
```

## Dependencies

No dependency manifest found.

## File Documentation

### QUICKSTART.md

#### Overview

# Quick Start Guide - AI Coding IDE

#### Module Overview

This file provides a step-by-step guide to get the AI Coding IDE up and running locally. It covers prerequisites, setup instructions, and initial steps to start using the platform.

#### Dependencies

- **Node.js 20+**: Required for running the backend and frontend.
- **Docker Desktop**: Needed to build and run Docker images for code execution.

#### Configuration

- **JWT_SECRET**: Set in `backend/.env`. Used for JWT authentication.
- **CLAUDE_API_KEY**: Optional. Set in `backend/.env` to enable real AI assistance.

#### Notes

- Ensure Docker Desktop is running before starting the backend.
- If ports are already in use, adjust the `PORT` in `backend/.env` and `frontend/vite.config.ts`.
- For real AI assistance, obtain a Claude API key and set it in `backend/.env`.
- If code execution fails, verify Docker images and ensure Docker is running.

### README.md

#### Overview

# AI-Powered Coding IDE Platform

Provides an overview of the AI-powered coding IDE, its features, tech stack, setup instructions, and project structure.

#### Module Overview

README.md serves as the entry point for developers, providing essential information about the project, setup instructions, and an overview of the tech stack and features.

#### Dependencies

- **Node.js + Express + TypeScript**: Backend framework and language.
- **Docker**: For secure, isolated code execution.
- **AWS DynamoDB**: Database for user data and projects.
- **Claude AI (Anthropic)**: Provides AI-powered code assistance.
- **React + TypeScript**: Frontend framework and language.
- **Monaco Editor**: Code editor component.
- **Axios**: HTTP client for API communication.
- **Tailwind CSS**: Utility-first CSS framework.

#### Configuration

- **.env**: Environment variables for both frontend and backend.
  - `JWT_SECRET`: Secret key for JWT authentication.
  - `CLAUDE_API_KEY`: API key for Claude AI.
  - `AWS_REGION`: AWS region (default: `us-east-1`).
  - `DYNAMODB_ENDPOINT`: DynamoDB endpoint (local: `http://localhost:8000`).

#### Notes

- Ensure Docker is installed and running for code execution.
- Set up environment variables correctly for both frontend and backend.
- Build Docker images for all supported languages before running the application.
- Use `docker-compose` for an easy setup of the development environment.
- Initialize the database using `npm run init-db` in the backend directory.
- For production deployment, configure environment variables and ensure Docker is available on the backend server.

### SETUP.md

#### Overview

# Setup Guide - AI-Powered Coding IDE

#### Module Overview

This file provides a comprehensive guide for setting up the AI-powered coding IDE platform from scratch, covering prerequisites, step-by-step setup, first-time usage, troubleshooting, development tips, and next steps.

#### Dependencies

- **Node.js** (v20 or higher): JavaScript runtime for executing backend and frontend code.
- **Docker Desktop**: Containerization platform for isolating code execution environments.
- **Git**: Version control system for cloning the repository.
- **Claude API Key**: Required for AI assistance features.

#### Configuration

### Backend Environment Variables
| Variable              | Description                                                                                     |
|-----------------------|-------------------------------------------------------------------------------------------------|
| `PORT`                | Server port (default: 3000)                                                                     |
| `NODE_ENV`            | Node environment (default: development)                                                          |
| `AWS_REGION`          | AWS region for DynamoDB (default: us-east-1)                                                    |
| `DYNAMODB_ENDPOINT`   | DynamoDB endpoint for local development (default: http://localhost:8000)                        |
| `JWT_SECRET`          | Secret key for JSON Web Tokens (change this)                                                    |
| `JWT_EXPIRES_IN`      | JWT expiration time (default: 24h)                                                              |
| `CLAUDE_API_KEY`      | Claude API key for AI service                                                                   |
| `CLAUDE_MODEL`        | Claude model version (default: claude-sonnet-4-20250514)                                         |
| `CORS_ORIGIN`         | Allowed origin for Cross-Origin Resource Sharing (default: http://localhost:5173)               |

### Frontend Environment Variables
| Variable              | Description                                                                                     |
|-----------------------|-------------------------------------------------------------------------------------------------|
| `VITE_API_BASE_URL`   | Base URL for API requests (default: http://localhost:3000/api)                                   |

#### Notes

- Ensure all environment variables are correctly set, especially `CLAUDE_API_KEY`.
- Verify Docker images are built and running before starting the backend server.
- Use hot reloading for efficient development workflow.
- Check logs for detailed error information during troubleshooting.

### docker-compose.yml

#### Overview

# Module Overview

The `docker-compose.yml` file defines and runs multi-container Docker applications. This file sets up the development environment for our project, including services for the backend, frontend, and a local DynamoDB instance.

# Dependencies

- **Docker**: Required to build and run the containers.
- **Docker Compose**: Needed to manage the multi-container setup.

# Services

| Service | Purpose | Key Configurations |
|---------|---------|---------------------|
| dynamodb-local | Local DynamoDB instance for development | Runs `amazon/dynamodb-local:latest`, exposes port 8000, uses a volume for data persistence |
| backend | Backend service for the application | Builds from `./backend/Dockerfile`, exposes port 3000, depends on `dynamodb-local` |
| frontend | Frontend service for the application | Builds from `./frontend/Dockerfile`, exposes port 5173, depends on `backend` |

# Networks

- **ai-ide-network**: A bridge network for inter-service communication.

# Volumes

- **dynamodb-data**: A volume for persisting DynamoDB data.

# Notes

- Ensure Docker and Docker Compose are installed and running on your machine.
- The `DYNAMODB_ENDPOINT` and `VITE_API_BASE_URL` environment variables are set to communicate between services.
- The `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY` are set to dummy values for local development. Update them for production use.
- The `docker.sock` is mounted to allow the backend service to interact with the Docker daemon.

### start-backend.ps1

#### Overview

# Module Overview

`start-backend.ps1` is a PowerShell script that sets up and starts the backend server for the AI Coding IDE project. It ensures the environment is correctly configured and dependencies are installed before launching the server.

#### Dependencies

- **npm**: Node.js package manager used to install dependencies.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `Write-Host` | `string`, `ForegroundColor` | None | Outputs messages to the console with specified color. |
| `Set-Location` | `string` | None | Changes the current directory to the specified path. |
| `Test-Path` | `string` | Boolean | Checks if a file or directory exists. |
| `Copy-Item` | `string`, `string` | None | Copies a file from one location to another. |
| `npm install` | None | None | Installs Node.js dependencies. |
| `npm run dev` | None | None | Starts the backend server in development mode. |

#### Notes

- The script checks for the existence of `.env` and `node_modules` directories.
- If `.env` is missing, it copies `.env.example` and prompts the user to set the `JWT_SECRET`.
- If `node_modules` is missing, it runs `npm install` to install dependencies.
- The backend server is started on port 3000 using `npm run dev`.

### start-frontend.ps1

#### Overview

# Module Overview

`start-frontend.ps1` automates the setup and startup of the frontend environment for the AI Coding IDE. It ensures the necessary environment files and dependencies are in place and starts the development server.

# Dependencies

- **npm**: Node package manager used to install dependencies and run scripts.

# Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `Write-Host` | `string`, `-ForegroundColor` | None | Outputs a message with specified foreground color. |
| `Set-Location` | `string` | None | Changes the current directory to the specified path. |
| `Test-Path` | `string` | Boolean | Checks if a specified path exists. |
| `Copy-Item` | `string`, `string` | None | Copies an item from a source to a destination. |
| `npm install` | None | None | Installs dependencies listed in `package.json`. |
| `npm run dev` | None | None | Starts the frontend development server. |

# Notes

- Ensure `npm` is installed and configured on the system where this script runs.
- The script checks for the existence of `.env` and `node_modules` directories and handles them accordingly.
- The frontend server starts on port 5173. Adjust the port if necessary in the `package.json` file.

### .kiro/specs/ai-powered-coding-ide/.config.kiro

#### Overview

# Module Overview

The **`.kiro/specs/ai-powered-coding-ide/.config.kiro`** file defines configuration settings for the AI-powered coding IDE feature. It specifies the unique identifier and the design-first workflow type for this feature, ensuring that the development aligns with the intended architecture and design principles.

#### Dependencies

- **None**: This file is self-contained and does not rely on external imports.

#### Configuration

| Key        | Value                  | Description                  |
|------------|------------------------|------------------------------|
| `specId`   | `924b5e80-42aa-4b4a-9814-ad12d9ad64cb` | Unique identifier for the feature. |
| `workflowType` | `design-first` | Indicates the workflow type for the feature. |

#### Notes

- The `specId` is crucial for tracking and managing the feature across different stages of development.
- The `workflowType` ensures that the feature development follows a design-first approach, emphasizing the importance of design principles in the architecture.

### .kiro/specs/ai-powered-coding-ide/requirements.md

#### Overview

# Module Overview

This file specifies the requirements for a complete, production-ready AI-powered coding IDE platform. It outlines the necessary features, security measures, and system components to deliver a robust user experience.

#### Dependencies

- **Monaco Editor**: For syntax highlighting and autocomplete in the code editor.
- **Docker**: For secure, isolated code execution environments.
- **Node.js/Express**: For backend API handling.
- **React**: For the frontend web interface.
- **DynamoDB**: For data persistence.
- **bcrypt**: For password hashing.
- **Claude API**: For AI-powered code assistance.

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `AuthSystem` | Manages user authentication and session handling | `registerUser`, `loginUser`, `logoutUser`, `validateSession` |
| `CodeEditor` | Handles code editing features | `initialize`, `updateSyntaxHighlighting`, `provideAutocomplete` |
| `ExecutionService` | Manages secure code execution in Docker containers | `executeCode`, `captureExecutionResults`, `terminateContainer` |
| `AIService` | Integrates with Claude API for AI code assistance | `processAIRequest`, `formatAIResponse` |
| `XPSystem` | Manages user experience points and achievements | `awardXP`, `levelUpUser`, `unlockAchievement` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `hashPassword` | `password: string` | `hashedPassword: string` | Hashes a user's password using bcrypt |
| `validateCodeSubmission` | `code: string` | `isValid: boolean` | Validates the format and size of a code submission |
| `sanitizeCodeInput` | `code: string` | `sanitizedCode: string` | Sanitizes code input to prevent injection attacks |
| `enforceResourceLimits` | `container: DockerContainer` | `void` | Enforces CPU, memory, and disk limits on a Docker container |
| `logExecutionAttempt` | `user: User, code: string` | `void` | Logs a code execution attempt with user context |

#### Configuration

- **Password Complexity Requirements**: Minimum 8 characters, mixed case, numbers.
- **Code Size Limit**: 100KB.
- **Execution Timeout**: 30 seconds.
- **Memory Limit**: 512MB.
- **AI Request Rate Limit**: 10 requests per minute per user.

#### Constants

- **SUPPORTED_LANGUAGES**: `['C++', 'Python', 'Java', 'JavaScript']`
- **BASE_IMAGES**: `{ cpp: 'gcc:latest', python: 'python:3.9-slim', java: 'openjdk:11-jdk-slim', js: 'node:14-slim' }`

#### Notes

- Ensure all passwords are hashed before storage using `bcrypt`.
- Implement robust error handling and logging throughout the system.
- Regularly update Docker images for security patches.
- Monitor AI service usage to manage costs and performance.

### .kiro/specs/ai-powered-coding-ide/tasks.md

#### Overview

# Module Overview

This file outlines the implementation plan for the AI-powered coding IDE platform, breaking down the project into actionable coding tasks. It serves as a roadmap for building the platform incrementally, ensuring each step results in working, testable code.

#### Dependencies

- **TypeScript**: For static typing and improved code quality.
- **Express**: For building the backend API.
- **React**: For the frontend user interface.
- **Docker**: For containerized code execution environments.
- **DynamoDB**: For data persistence.
- **Claude AI**: For AI-powered code assistance.

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `ExecutionService` | Manages code execution in Docker containers. | `executeCode()` |
| `AIService` | Interfaces with the Claude AI API for code assistance. | `assistWithCode()` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `hashPassword` | `password: string` | `Promise<string>` | Hashes a password using bcrypt. |
| `comparePassword` | `password: string, hash: string` | `Promise<boolean>` | Compares a password with its hash. |
| `generateToken` | `userId: string, username: string` | `string` | Generates a JWT token for authentication. |
| `verifyToken` | `token: string` | `Promise<any>` | Verifies a JWT token. |
| `authenticateToken` | `req: Request, res: Response, next: NextFunction` | `void` | Middleware to authenticate requests using JWT. |

#### Configuration

- **.env.example**: Template for environment variables, including API keys and database configurations.
- **tsconfig.json**: TypeScript configuration with strict mode and ES2020 target.
- **tailwind.config.js**: Configuration for Tailwind CSS.

#### Constants

- **MAX_CODE_SIZE**: Maximum allowed size for code submissions (100KB).
- **BCRYPT_SALT_ROUNDS**: Number of salt rounds for bcrypt (10).
- **JWT_EXPIRATION**: Token expiration time (24 hours).
- **EXECUTION_TIMEOUT**: Timeout for code execution (30 seconds).
- **MEMORY_LIMIT**: Memory limit for code execution containers (512MB).

#### Notes

- Ensure all dependencies are installed and configured before starting development.
- Follow the task order for incremental building and testing.
- Pay attention to security configurations, especially for code execution and AI interactions.
- Use environment variables for sensitive information like API keys and database credentials.
- Regularly review and update the implementation plan as the project evolves.

### backend/.dockerignore

#### Overview

# Module Overview

The `backend/.dockerignore` file specifies which files and directories Docker should ignore when building the image for the backend service. This helps to keep the image size small and avoids including unnecessary files that could cause issues during the build process.

# Dependencies

No direct dependencies are listed here. This file is a simple text file that relies on Docker's built-in ignore functionality.

# Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

# Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| N/A | N/A | N/A | N/A |

# Configuration or Constants

No configuration or constants are defined in this file.

# Notes

- Ensure that any new directories or files that should not be included in the Docker image are added to this file.
- Be cautious with patterns; overly broad patterns might exclude necessary files.
- This file complements `.dockerignore` at the root level, so ensure consistency between the two if they overlap in purpose.

### backend/.env.example

#### Overview

# Backend Configuration File

This file, `backend/.env.example`, provides a template for environment variables used to configure the backend services. It includes settings for server, database, authentication, and optional AI service configurations.

#### Dependencies

- **dotenv**: Used to load environment variables from a `.env` file into `process.env`.

#### Configuration

| Key | Purpose |
| --- | --- |
| PORT | Sets the port number for the server. |
| NODE_ENV | Defines the environment for the Node.js application (development, production, etc.). |
| AWS_REGION | Specifies the AWS region for database services. |
| AWS_ACCESS_KEY_ID | AWS access key for authentication. |
| AWS_SECRET_ACCESS_KEY | AWS secret key for authentication. |
| DYNAMODB_ENDPOINT | Endpoint for the DynamoDB service. |
| DYNAMODB_TABLE_NAME | Name of the DynamoDB table used. |
| JWT_SECRET | Secret key for JSON Web Token (JWT) signing. |
| JWT_EXPIRES_IN | Duration for which the JWT is valid. |
| CLAUDE_API_KEY | API key for the optional AI service. |
| CLAUDE_MODEL | Model to use for the AI service. |
| CORS_ORIGIN | Allowed origin for Cross-Origin Resource Sharing (CORS). |
| DOCKER_HOST | Docker socket path for Docker operations. |
| RATE_LIMIT_WINDOW_MS | Time window for rate limiting. |
| RATE_LIMIT_MAX_REQUESTS | Maximum number of requests allowed in the rate limit window. |
| AI_RATE_LIMIT_MAX_REQUESTS | Maximum number of AI service requests allowed in the rate limit window. |
| CODE_EXEC_RATE_LIMIT_MAX_REQUESTS | Maximum number of code execution requests allowed in the rate limit window. |
| CODE_TIMEOUT_MS | Timeout for code execution in milliseconds. |
| CODE_MAX_SIZE_BYTES | Maximum size of code to be executed in bytes. |
| CONTAINER_MEMORY_LIMIT_MB | Memory limit for code execution containers in MB. |

#### Notes

- Always replace placeholder values (`your_access_key_here`, `your_super_secret_jwt_key_change_this_in_production`, etc.) with actual values before deploying to production.
- The `CLAUDE_API_KEY` and `CLAUDE_MODEL` are optional and used if the AI service is configured.
- Ensure that sensitive information like `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY` are stored securely and not committed to version control.

### backend/Dockerfile

#### Overview

# Backend Dockerfile

This file defines a Docker image for the backend service of our application. It sets up the environment, installs dependencies, and configures the application to run.

#### Dependencies

- `node:20-alpine`: A lightweight Node.js base image.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `RUN npm install` | N/A | Installs dependencies listed in `package.json` |

#### Notes

- The `WORKDIR /app` sets the working directory inside the container to `/app`.
- The `EXPOSE 3000` command indicates that the container listens on port 3000.
- The `CMD ["npm", "run", "dev"]` command starts the development server using npm.

### backend/package.json

#### Overview

### Module Overview

The `backend/package.json` file is the heart of our backend project's configuration. It lists the project's metadata, dependencies, and scripts necessary for development and deployment.

### Dependencies

| Dependency | Purpose |
|------------|---------|
| `express` | Web framework for Node.js. |
| `cors` | Middleware to enable Cross-Origin Resource Sharing. |
| `helmet` | Middleware to secure Express apps by setting various HTTP headers. |
| `dotenv` | Module to load environment variables from a `.env` file into `process.env`. |
| `bcryptjs` | Library for hashing passwords. |
| `jsonwebtoken` | Library for creating and verifying JSON Web Tokens. |
| `@anthropic-ai/sdk` | SDK for interacting with Anthropic's AI services. |
| `dockerode` | Library for controlling Docker from Node.js. |
| `@aws-sdk/client-dynamodb` | AWS SDK client for DynamoDB. |
| `@aws-sdk/lib-dynamodb` | AWS SDK library for DynamoDB. |
| `winston` | Logger for Node.js. |
| `express-rate-limit` | Middleware for rate limiting requests. |
| `express-validator` | Middleware for validating request data. |
| `uuid` | Library for generating UUIDs. |
| `node-cache` | In-memory caching for Node.js. |

### Scripts

| Script | Description |
|--------|-------------|
| `dev` | Starts the development server with hot-reloading. |
| `build` | Compiles TypeScript files to JavaScript. |
| `start` | Starts the production server. |
| `init-db` | Initializes the database. |
| `test` | Runs the test suite. |
| `lint` | Lints TypeScript files. |
| `lint:fix` | Lints and automatically fixes TypeScript files. |

### Notes

- Ensure the `.env` file is properly configured before running the server.
- The `build` script should be run before the `start` script to ensure the latest code is deployed.
- The `test` script should be run regularly to maintain code quality.

### backend/tsconfig.json

#### Overview

# Backend `tsconfig.json` Overview

This file configures the TypeScript compiler for the backend codebase. It sets compiler options that dictate how TypeScript files should be compiled, ensuring consistency and enabling advanced TypeScript features.

#### Dependencies

This file relies on TypeScript to compile TypeScript files (`.ts`) into JavaScript (`.js`), providing a robust type-checking environment.

#### Configuration

| Option | Purpose |
| --- | --- |
| `target` | Sets the ECMAScript target version (`ES2020`). |
| `module` | Specifies the module code generation (`commonjs`). |
| `lib` | Includes library files (`ES2020`). |
| `outDir` | Specifies the directory where compiled files are output (`./dist`). |
| `rootDir` | Specifies the root directory of source files (`./src`). |
| `strict` | Enables all strict type-checking options. |
| `esModuleInterop` | Enables interoperability between CommonJS and ES Modules. |
| `skipLibCheck` | Skips type checking of all declaration files (`.d.ts`). |
| `forceConsistentCasingInFileNames` | Ensures that file names are case-consistent across platforms. |
| `resolveJsonModule` | Allows importing `.json` files. |
| `moduleResolution` | Specifies the module resolution strategy (`node`). |
| `declaration` | Generates `.d.ts` files. |
| `declarationMap` | Generates a source map for `.d.ts` files. |
| `sourceMap` | Generates source maps for `.js` files. |
| `noUnusedLocals` | Reports errors on unused local variables. |
| `noUnusedParameters` | Reports errors on unused parameters. |
| `noImplicitReturns` | Reports error on functions that do not return a value. |
| `noFallthroughCasesInSwitch` | Reports error on switch cases without default. |

#### Notes

- The `strict` option is enabled to ensure type safety.
- `noUnusedLocals` and `noUnusedParameters` are set to `false` to allow for flexibility in development.
- Source maps are generated for easier debugging.
- The `resolveJsonModule` option allows importing JSON files directly as modules.

### backend/docker-images/cpp/Dockerfile

#### Overview

# Module Overview

This Dockerfile builds a Docker image for running C++ applications in an Alpine Linux environment. It sets up a non-root user for security and defines the working directory for application code.

# Dependencies

- `gcc:13-alpine`: Base image with GCC compiler for C++ development.

# Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

# Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `addgroup -S coderunner` | None | None | Creates a new group named `coderunner`. |
| `adduser -S coderunner -G coderunner` | None | None | Creates a new user named `coderunner` and adds them to the `coderunner` group. |
| `WORKDIR /code` | None | None | Sets the working directory to `/code`. |
| `chown -R coderunner:coderunner /code` | None | None | Changes ownership of `/code` to the `coderunner` user and group. |
| `USER coderunner` | None | None | Switches to the `coderunner` non-root user for running commands. |
| `CMD ["/bin/sh"]` | None | None | Default command to start a shell when the container runs. |

# Configuration

- The image uses Alpine Linux for a minimal footprint.
- A non-root user `coderunner` is created for running applications securely.

# Notes

- The default command is a shell, which allows for interactive use of the container.
- Ensure to specify the appropriate build and run commands in your Docker Compose or deployment scripts.
- For production use, consider setting an entrypoint script to run your C++ application directly.

### backend/docker-images/java/Dockerfile

#### Overview

# Module Overview

This Dockerfile builds a Docker image for running a Java application. It sets up a minimal Java environment, creates a non-root user for security, configures the working directory, and specifies the default command to run the Java application.

# Dependencies

- `openjdk:21-slim`: A lightweight Docker image with OpenJDK 21 for running Java applications.

# Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | This Dockerfile does not define any classes. | N/A |

# Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `FROM` | `openjdk:21-slim` | Base image | Specifies the base image for the Docker container. |
| `RUN groupadd -r coderunner && useradd -r -g coderunner coderunner` | None | Creates a non-root user and group. |
| `WORKDIR /code` | None | Sets the working directory inside the container. |
| `RUN chown -R coderunner:coderunner /code` | None | Changes ownership of the working directory to the non-root user. |
| `USER coderunner` | None | Switches to the non-root user for running processes. |
| `CMD ["java"]` | None | Specifies the default command to run when the container starts. |

# Configuration

- `coderunner`: Non-root user created for running the application securely.
- `/code`: Working directory where the application will be placed and executed.

# Notes

- Ensure that any application JAR files or dependencies are copied into the `/code` directory when building the Docker image.
- The default command `CMD ["java"]` assumes that a `main` class is defined in the application. Adjust if necessary.
- This image uses a minimal OpenJDK 21 base to keep the container size small.

### backend/docker-images/javascript/Dockerfile

#### Overview

# Module Overview

This Dockerfile sets up a Docker image for running JavaScript applications using the Node.js runtime. It creates a non-root user for security reasons and sets the working directory to `/code`.

# Dependencies

- `node:20-alpine`: The base image that includes Node.js 20 and Alpine Linux for a lightweight container.

# Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

# Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `addgroup -S coderunner` | N/A | N/A | Creates a new group named `coderunner`. |
| `adduser -S coderunner -G coderunner -G` | N/A | N/A | Creates a new user named `coderunner` and adds it to the `coderunner` group. |
| `chown -R coderunner:coderunner /code` | N/A | N/A | Changes ownership of `/code` directory to `coderunner`. |
| `CMD ["node"]` | N/A | N/A | Sets the default command to run `node`. |

# Configuration

- The image uses `node:20-alpine` which includes Node.js 20 and Alpine Linux for a minimal footprint.
- A non-root user `coderunner` is created to run the application, enhancing security.

# Notes

- The `USER coderunner` command switches to the non-root user for running the application.
- The working directory `/code` is set and ownership is changed to the `coderunner` user.
- Ensure that any application code is copied into the `/code` directory during the Docker build process.

### backend/docker-images/python/Dockerfile

#### Overview

# Module Overview

This Dockerfile sets up a Python 3.12 environment in an Alpine Linux container. It creates a non-root user for security purposes, sets the working directory to `/code`, and switches to this non-root user for running processes.

# Dependencies

This Dockerfile does not import any external libraries or dependencies beyond the base Python image.

# Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

# Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| FROM | `python:3.12-alpine` | Base image | Specifies the base image for the container. |
| RUN | `addgroup -S coderunner && adduser -S coderunner -G coderunner` | None | Creates a non-root user named `coderunner`. |
| WORKDIR | `/code` | None | Sets the working directory to `/code`. |
| RUN | `chown -R coderunner:coderunner /code` | None | Changes ownership of `/code` to `coderunner`. |
| USER | `coderunner` | None | Switches to the non-root user `coderunner` for running processes. |
| CMD | `["python3"]` | None | Sets the default command to run Python 3. |

# Configuration

No configuration settings are defined in this Dockerfile.

# Notes

- The container uses Alpine Linux for a minimal and secure environment.
- The non-root user `coderunner` is used to prevent potential security risks.
- The default command `CMD ["python3"]` allows for running Python scripts within the container.

### backend/scripts/build-images.ps1

#### Overview

# Module Overview

This PowerShell script automates the building of Docker images for various programming languages used in code execution. It ensures all necessary images are built and ready for deployment.

#### Dependencies

- **Docker CLI**: Required for building Docker images.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `docker build` | `-t <tag> <path>` | Docker image | Builds a Docker image from a Dockerfile and associated files in the specified path. |

#### Notes

- The script navigates to the `docker-images` directory and builds images for Python, JavaScript, C++, and Java.
- It uses the `docker build` command to create images with specific tags.
- After building, it prints a success message and suggests verifying the images using `docker images | Select-String "code-executor"`.

### backend/scripts/build-images.sh

#### Overview

# Module Overview

This script automates the building of Docker images for executing code in different programming languages. It's crucial for ensuring that the development environment is consistently prepared with the necessary container images.

#### Dependencies

This script relies on Docker to build and manage the images.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `build-image` | `language_dir` | `None` | Builds a Docker image for the specified language. |

#### Configuration

No configuration is required for this script. It assumes the presence of specific directories within the `docker-images` folder.

#### Notes

- Ensure Docker is running and accessible before executing this script.
- The script outputs the status of each image build and lists all built images at the end.
- Verify the built images using the command `docker images | grep code-executor`.

### backend/scripts/init-db.ts

#### Overview

# backend/scripts/init-db.ts

#### Module Overview

This file initializes the DynamoDB table required for our application. It checks if the table already exists and creates it if it doesn't.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `DynamoDBClient`, `CreateTableCommand`, `ListTablesCommand` from `@aws-sdk/client-dynamodb` | Provides the necessary AWS SDK commands to interact with DynamoDB. |
| `dotenv` | Loads environment variables from a `.env` file into `process.env`. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `createTable` | None | None | Checks if the DynamoDB table exists and creates it if it doesn't. |

#### Notes

- The script uses dummy credentials for AWS access. Ensure to replace them with actual credentials in a production environment.
- The script assumes the environment variables `AWS_REGION`, `DYNAMODB_ENDPOINT`, and `DYNAMODB_TABLE_NAME` are set. If not, it defaults to `us-east-1`, `http://localhost:8000`, and `CodingIDETable`, respectively.
- The script waits for 2 seconds after creating the table to ensure it's active before proceeding. This might need adjustment based on actual table creation times.

### backend/src/server.ts

#### Overview

# backend/src/server.ts

#### Module Overview

This file initializes and configures the Express server, sets up middleware, defines routes, and handles server lifecycle events.

#### Dependencies

| Import | Purpose |
| ------ | ------- |
| `express` | Web framework for Node.js |
| `cors` | Middleware to enable Cross-Origin Resource Sharing |
| `helmet` | Middleware to set various HTTP headers for security |
| `dotenv` | Module to load environment variables from a `.env` file |
| `logger` | Custom logger from `./utils/logger` |
| `errorHandler` | Custom middleware for handling errors |
| `requestLogger` | Custom middleware for logging requests |
| `authRoutes`, `codeRoutes`, `aiRoutes`, `projectRoutes`, `userRoutes` | Route handlers for different parts of the API |

#### Functions

| Function | Parameters | Returns | Description |
| -------- | ---------- | ------- | ----------- |
| `app.get('/health',...)` | `_req`, `res` | `void` | Returns a health check response |

#### Configuration

- `PORT`: Server port, defaults to 3000 if not set in environment variables.
- `CORS_ORIGIN`: Origin for CORS, defaults to `http://localhost:5173`.

#### Notes

- Ensure the `.env` file is properly configured with necessary environment variables.
- The server listens on the port specified by `PORT` or defaults to 3000.
- Graceful shutdown is implemented to handle `SIGTERM` and `SIGINT` signals.

### backend/src/config/dynamodb.ts

#### Overview

# Backend Configuration for DynamoDB

This file sets up the DynamoDB client and exports it for use throughout the application. It also defines the table name to be used.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `DynamoDBClient` | Provides the core DynamoDB functionality. |
| `DynamoDBDocumentClient` | Adds higher-level document API on top of the low-level DynamoDB client. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `DynamoDBClient` | Manages DynamoDB interactions. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `DynamoDBDocumentClient.from` | `DynamoDBClient`, `marshallOptions` | `DynamoDBDocumentClient` | Creates a document client from a DynamoDB client with specified marshalling options. |

#### Configuration

| Constant | Purpose |
| --- | --- |
| `TABLE_NAME` | The name of the DynamoDB table to use. Defaults to `CodingIDETable` if not specified in environment variables. |

#### Notes

- The `DynamoDBClient` is configured to use environment variables for region and endpoint.
- Credentials are hardcoded as placeholders if an endpoint is provided. In a production environment, these should be securely managed.
- The `dynamoDb` export is the document client configured with marshalling options to remove undefined values and convert class instances to maps.

### backend/src/middleware/auth.ts

#### Overview

# Backend Middleware: `auth.ts`

#### Module Overview

This file defines middleware for token-based authentication in the backend. It verifies the presence and validity of an authentication token in incoming requests, ensuring that only authenticated users can access protected routes.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Request`, `Response`, `NextFunction` | From `express`, used for defining the request, response, and next function in middleware. |
| `verifyToken` | From `../utils/auth`, used to validate the authentication token. |
| `AppError` | From `./errorHandler`, used to create custom error responses. |
| `AuthRequest` | From `../types`, a custom request type that extends the default Express request object to include user information. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `authenticateToken` | `req: Request`, `_res: Response`, `next: NextFunction` | `void` | Verifies the authentication token in the request headers and attaches the user object to the request if valid. |

#### Notes

- The `authenticateToken` function expects the token to be passed in the `Authorization` header as a Bearer token.
- If no token is provided or the token is invalid/expired, it passes a `401 Unauthorized` error to the next middleware.
- The `verifyToken` function throws an error if the token is invalid or expired, which is caught and handled by returning a `401 Unauthorized` error.
- The user object is attached to the request as `(req as AuthRequest).user` for use in subsequent middleware or route handlers.

### backend/src/middleware/errorHandler.ts

#### Overview

# backend/src/middleware/errorHandler.ts

#### Module Overview

This file defines a custom error handling middleware for the Express application. It ensures that errors are logged and appropriate responses are sent to the client.

#### Dependencies

| Import | Purpose |
|--------|----------|
| `Request`, `Response`, `NextFunction` from 'express' | Types for Express request, response, and next function. |
| `logger` from '../utils/logger' | Custom logger utility for logging errors. |

#### Classes

| Class | Purpose | Key Methods |
|-------|---------|-------------|
| `AppError` | Custom error class to handle operational errors with a status code. | `constructor(message: string, statusCode: number)` |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `errorHandler` | `err: Error | AppError`, `req: Request`, `res: Response`, `_next: NextFunction` | `void` | Middleware function to handle errors and send appropriate responses. |

#### Notes

- The `AppError` class extends the built-in `Error` class and adds a `statusCode` and `isOperational` property to differentiate operational errors.
- The `errorHandler` function checks the type of error and sends a response based on the error type.
- Ensure that all errors thrown in the application are either instances of `AppError` or have the correct `name` property for validation and JWT errors to be handled properly.

### backend/src/middleware/requestLogger.ts

#### Overview

# Backend Module Documentation: `backend/src/middleware/requestLogger.ts`

#### Module Overview

This file defines a middleware function that logs incoming HTTP requests and their processing times. It captures essential details like request method, path, user ID, and IP address, and logs both the start and completion of each request.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Request`, `Response`, `NextFunction` | Types from Express for request, response, and next function. |
| `logger` | Custom logger utility for logging messages. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `requestLogger` | `req: Request`, `res: Response`, `next: NextFunction` | `void` | Logs incoming requests and their completion times. |

#### Notes

- The `requestLogger` function captures the start time of each request and logs it along with the method and path.
- It also logs the completion of the request, including the response status and duration.
- The user ID is extracted from the request object, assuming it's available under `(req as any).user.id`.
- The `res.on('finish')` event ensures that the completion log is emitted when the response is fully sent to the client.
- Be cautious with type assertions like `(req as any).user.id` as they bypass TypeScript's type checking. Ensure that the user object is indeed present in the request.

### backend/src/routes/ai.ts

#### Overview

# backend/src/routes/ai.ts

#### Module Overview

This file defines the routes for AI-related functionalities, specifically an endpoint for AI-assisted code help. It handles incoming requests for code assistance, validates the input, and delegates the processing to the AI service.

#### Dependencies

| Import | Purpose |
|--------|---------|
| `Router`, `Response`, `NextFunction` from `express` | Core Express components for defining routes and handling requests. |
| `body`, `validationResult` from `express-validator` | Middleware for validating request bodies. |
| `aiService` from `../services/aiService` | Service layer for interacting with the AI. |
| `AppError` from `../middleware/errorHandler` | Custom error handling for Express. |
| `AIAction`, `CodeContext` from `../types` | Type definitions for AI actions and code context. |
| `logger` from `../utils/logger` | Utility for logging information and errors. |
| `rateLimit` from `express-rate-limit` | Middleware for rate limiting requests. |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `router.post('/assist',...)` | `req: any`, `res: Response`, `next: NextFunction` | `void` | Handles POST requests for AI assistance, validates input, and processes the request with the AI service. |

#### Notes

- The rate limiter is set to allow a maximum of 10 requests per minute per IP address.
- The endpoint does not require authentication, making it accessible for demonstration purposes.
- Error handling is done using the `AppError` class to ensure consistent error responses.
- The `logger` is used to log information and errors, which can be helpful for debugging and monitoring.

### backend/src/routes/auth.ts

#### Overview

# backend/src/routes/auth.ts

This file defines the authentication routes for the application, handling user registration, login, and logout.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Router`, `Request`, `Response`, `NextFunction` from `express` | To create and manage HTTP routes and handle requests and responses. |
| `body`, `validationResult` from `express-validator` | For validating request data. |
| `createUser`, `getUserByUsername`, `initializeUserStats` from `../services/database` | To interact with the user database. |
| `hashPassword`, `comparePassword`, `generateToken`, `validatePassword`, `validateEmail` from `../utils/auth` | To handle password hashing, comparison, token generation, and validation. |
| `AppError` from `../middleware/errorHandler` | To create custom error responses. |
| `logger` from `../utils/logger` | To log important events and errors. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `router.post('/register',...)` | `req: Request`, `res: Response`, `next: NextFunction` | `void` | Handles user registration, validates input, checks for existing users, hashes passwords, and creates a new user. |
| `router.post('/login',...)` | `req: Request`, `res: Response`, `next: NextFunction` | `void` | Handles user login, validates input, checks user existence, compares passwords, and generates a token. |
| `router.post('/logout',...)` | `req: Request`, `res: Response` | `void` | Logs the user out and logs the logout event. |

#### Notes

- The `/register` endpoint requires a username, email, and password. It validates the input, checks for existing users, and hashes the password before creating a new user.
- The `/login` endpoint requires a username and password. It validates the input, checks for user existence, and compares the provided password with the stored hash.
- The `/logout` endpoint logs the logout event and responds with a success message. Note that the actual token removal is handled client-side.
- Always ensure sensitive information like passwords are handled securely and never logged directly.

### backend/src/routes/code.ts

#### Overview

# backend/src/routes/code.ts

#### Module Overview

This file defines the routes for code execution in our backend. It allows users to submit code snippets for execution, specifying the programming language and optional input. The routes handle validation, rate limiting, and delegate the execution to a service.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Router`, `Response`, `NextFunction` from `express` | To create and manage HTTP routes |
| `body`, `validationResult` from `express-validator` | For request body validation |
| `executionService` from `../services/executionService` | To execute the submitted code |
| `AppError` from `../middleware/errorHandler` | To handle and pass custom errors |
| `SupportedLanguage` from `../types` | To define supported programming languages |
| `logger` from `../utils/logger` | To log information and errors |
| `rateLimit` from `express-rate-limit` | To limit the rate of code execution requests |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `router.post('/execute',...)` | `req`, `res`, `next` | `void` | Handles POST requests to execute code, performs validation, rate limiting, and delegates execution |

#### Notes

- The `/execute` endpoint does not require authentication for demo purposes.
- The rate limiter allows up to 20 requests per minute per IP address.
- The maximum allowed code size is configurable via the `CODE_MAX_SIZE_BYTES` environment variable.
- The supported programming languages are hardcoded in the validation middleware.
- Errors are handled using the `AppError` class, which ensures consistent error responses.

### backend/src/routes/projects.ts

#### Overview

# backend/src/routes/projects.ts

#### Module Overview

This file defines the Express routes for managing projects, including creating, reading, updating, and deleting projects. It integrates with authentication middleware and validation to ensure secure and accurate project operations.

#### Dependencies

| Import | Purpose |
| ------ | ------- |
| `Router`, `Response`, `NextFunction` from `express` | Core Express functionalities for defining routes and handling requests. |
| `body`, `param`, `validationResult` from `express-validator` | Tools for validating request data. |
| `authenticateToken` from `../middleware/auth` | Middleware to authenticate requests using a token. |
| `createProject`, `getUserProjects`, `getProjectById`, `updateProject`, `deleteProject` from `../services/database` | Functions to interact with the database for project operations. |
| `AppError` from `../middleware/errorHandler` | Custom error handling for Express. |
| `AuthRequest` from `../types` | Type definition for requests with authenticated user data. |
| `logger` from `../utils/logger` | Utility for logging information and errors. |

#### Functions

| Route | Method | Path | Middleware | Handler |
| ----- | ------ | ---- | ---------- | ------- |
| Get User Projects | GET | `/` | `authenticateToken` | Retrieves all projects for the authenticated user. |
| Create Project | POST | `/` | `authenticateToken` | Creates a new project with validation. |
| Update Project | PUT | `/:id` | `authenticateToken` | Updates an existing project with validation and ownership check. |
| Delete Project | DELETE | `/:id` | `authenticateToken` | Deletes a project with validation and ownership check. |

#### Notes

- All routes require authentication via the `authenticateToken` middleware.
- Validation is performed using `express-validator` for both body and parameter data.
- Error handling is centralized using `AppError` to ensure consistent error responses.
- Logging is used to track project creation, updates, and deletions for auditing purposes.

### backend/src/routes/user.ts

#### Overview

# Backend Module Documentation: `backend/src/routes/user.ts`

#### Module Overview

This file defines the Express routes for handling user-related requests. It primarily focuses on fetching user statistics after validating the user's authentication token.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Router`, `Response`, `NextFunction` | Core Express modules for defining routes and handling HTTP responses. |
| `authenticateToken` | Middleware for validating user authentication tokens. |
| `getUserStats` | Service function to retrieve user statistics from the database. |
| `AppError` | Custom error handling class for API error responses. |
| `AuthRequest` | Type definition for requests with authenticated user information. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `router.get` | `path: '/stats'`, `middleware: [authenticateToken, async handler]`, `res: Response`, `next: NextFunction` | `res.json({ stats })` | Defines a GET route for fetching user statistics. Uses `authenticateToken` middleware to validate the user's token and `getUserStats` to fetch the user's stats. If stats are not found, it returns a 404 error. |

#### Notes

- Ensure that the user's authentication token is valid and includes the user ID before accessing this route.
- Any errors during the fetching of user stats are passed to the next error handler.
- The route expects the user ID to be present in the request object (`req.user.id`).

### backend/src/services/aiService.ts

#### Overview

# Module Overview

This file defines the `AIService` class, which provides an interface for interacting with the Anthropic Claude API to generate code-related responses and suggestions. It handles both real API calls and mock responses when no API key is configured.

#### Dependencies

- `@anthropic-ai/sdk`: The official SDK for interacting with the Anthropic Claude API.
- `../types`: Custom types used throughout the application.
- `../utils/logger`: A custom logger utility for logging events and errors.

#### Classes

| Class     | Purpose                                                                                       | Key Methods                                                                                     |
| --------- | --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| `AIService` | Provides methods for generating AI-assisted code responses and suggestions.                   | `assistWithCode`, `mockAssistWithCode`, `buildSystemPrompt`, `buildUserPrompt`, `extractCodeSuggestions` |

#### Functions

| Function                  | Parameters                                                                                           | Returns                                                                                              | Description                                                                                       |
| ------------------------- | ---------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| `assistWithCode`          | `query: string`, `codeContext: CodeContext`, `action: AIAction`                                      | `Promise<{ response: string; suggestions: CodeSuggestion[] }>`                                       | Generates an AI-assisted response and code suggestions based on the provided query and context.   |
| `mockAssistWithCode`      | `query: string`, `codeContext: CodeContext`, `action: AIAction`                                      | `{ response: string; suggestions: CodeSuggestion[] }`                                               | Generates a mock AI response and suggestions when no API key is configured.                       |
| `buildSystemPrompt`       | `action: AIAction`                                                                                   | `string`                                                                                            | Constructs the system prompt based on the specified AI action.                                    |
| `buildUserPrompt`         | `query: string`, `codeContext: CodeContext`, `action: AIAction`                                      | `string`                                                                                            | Constructs the user prompt based on the query, code context, and AI action.                        |
| `extractCodeSuggestions`  | `response: string`, `action: AIAction`                                                               | `CodeSuggestion[]`                                                                                  | Extracts code suggestions from the AI response.                                                   |

#### Configuration

- `USE_MOCK`: A boolean indicating whether to use the mock AI service (true if no API key is configured).
- `anthropic`: An instance of the Anthropic SDK, initialized only if `USE_MOCK` is false.
- `MODEL`: The Claude model to use for API requests, defaulting to `'claude-sonnet-4-20250514'`.

#### Notes

- The `assistWithCode` method checks if `USE_MOCK` is true and calls `mockAssistWithCode` if so.
- The `anthropic` instance is accessed using the non-null assertion operator (`!`), assuming it is initialized when `USE_MOCK` is false.
- Mock responses are stored in the `mockResponses` object within `mockAssistWithCode`.
- Code suggestions are extracted from markdown code blocks in the AI response using a regular expression.

### backend/src/services/database.ts

#### Overview

# Module Overview

This file contains functions for interacting with our DynamoDB database. It handles CRUD operations for users, projects, and user stats, as well as logging execution history.

#### Dependencies

- `@aws-sdk/lib-dynamodb`: Provides commands for interacting with DynamoDB.
- `dynamoDb` and `TABLE_NAME` from `../config/dynamodb`: Configuration for our DynamoDB client and table name.
- `User`, `Project`, `UserStats`, `ExecutionOutput` from `../types`: TypeScript interfaces for our data models.
- `uuidv4` from 'uuid': Generates unique IDs for new records.

#### Functions

| Function                  | Parameters                                                                                       | Returns                 | Description                                                                                           |
|---------------------------|--------------------------------------------------------------------------------------------------|-------------------------|-------------------------------------------------------------------------------------------------------|
| `createUser`              | `username: string`, `email: string`, `passwordHash: string`                                      | `Promise<User>`         | Creates a new user record in DynamoDB.                                                                |
| `getUserByUsername`       | `username: string`                                                                               | `Promise<User | null>`  | Retrieves a user record by username.                                                                  |
| `getUserById`             | `userId: string`                                                                                 | `Promise<User | null>`  | Retrieves a user record by ID.                                                                        |
| `createProject`           | `userId: string`, `name: string`, `code: string`, `language: string`                             | `Promise<Project>`      | Creates a new project record for a user.                                                              |
| `getUserProjects`         | `userId: string`                                                                                 | `Promise<Project[]>`    | Retrieves all projects for a user.                                                                    |
| `getProjectById`          | `projectId: string`                                                                              | `Promise<Project | null>` | Retrieves a project record by ID.                                                                     |
| `updateProject`           | `projectId: string`, `userId: string`, `updates: Partial<Project>`                               | `Promise<void>`         | Updates a project record with provided fields.                                                        |
| `deleteProject`           | `projectId: string`, `userId: string`                                                            | `Promise<void>`         | Deletes a project record.                                                                             |
| `getUserStats`            | `userId: string`                                                                                 | `Promise<UserStats | null>` | Retrieves user stats record.                                                                          |
| `initializeUserStats`     | `userId: string`                                                                                 | `Promise<UserStats>`    | Initializes a new user stats record with default values.                                              |
| `updateUserStats`         | `userId: string`, `updates: Partial<UserStats>`                                                  | `Promise<void>`         | Updates user stats record with provided fields.                                                       |
| `logExecution`            | `userId: string`, `code: string`, `language: string`, `output: ExecutionOutput`                 | `Promise<void>`         | Logs an execution event to DynamoDB.                                                                  |

#### Notes

- All functions that interact with DynamoDB use the `dynamoDb` client configured in `../config/dynamodb`.
- Unique IDs for new records are generated using `uuidv4`.
- Update functions construct dynamic update expressions based on provided fields.
- Execution history items are keyed by a combination of user ID and timestamp to ensure uniqueness.

### backend/src/services/executionService.ts

#### Overview

# Module Overview
This file contains the `ExecutionService` class, which handles the execution of user-submitted code in isolated Docker containers. It supports multiple programming languages and ensures that each execution is sandboxed, with resource limits and timeouts in place.

#### Dependencies

- `Docker` from `dockerode`: Interacts with Docker to create and manage containers.
- `SupportedLanguage`, `ExecutionOutput` from `../types`: Type definitions for supported languages and execution results.
- `logger` from `../utils/logger`: Logging utility.
- `writeFile`, `unlink` from `fs/promises`: File system operations.
- `join` from `path`: Path manipulation.
- `tmpdir` from `os`: Temporary directory utility.
- `v4 as uuidv4` from `uuid`: Generates unique identifiers.

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `ExecutionService` | Manages code execution in Docker containers. | `executeCode`, `createContainer`, `createTarArchive`, `createTimeout`, `extractStdout`, `extractStderr` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `executeCode` | `code: string`, `language: SupportedLanguage`, `stdin: string`, `timeout: number` | `Promise<ExecutionOutput>` | Executes the provided code in a Docker container. |
| `createContainer` | `code: string`, `language: SupportedLanguage`, `stdin: string`, `config: LanguageConfig` | `Promise<Docker.Container>` | Creates a Docker container for code execution. |
| `createTarArchive` | `fileName: string`, `content: Buffer` | `Promise<Buffer>` | Creates a tar archive containing the code file. |
| `createTimeout` | `ms: number` | `Promise<never>` | Creates a promise that rejects after a specified timeout. |
| `extractStdout` | `logs: string` | `string` | Extracts stdout from Docker logs. |
| `extractStderr` | `logs: string` | `string` | Extracts stderr from Docker logs. |

#### Configuration

- `TIMEOUT_MS`: Execution timeout in milliseconds, defaults to 30000.
- `MEMORY_LIMIT_MB`: Memory limit for containers in megabytes, defaults to 512.
- `MEMORY_LIMIT_BYTES`: Memory limit in bytes, calculated from `MEMORY_LIMIT_MB`.

#### Constants

- `languageConfigs`: Configuration for supported languages, including Docker image, file extension, and execution commands.

#### Notes

- Ensure Docker is running and the required images are available.
- Resource limits and timeouts are enforced to prevent abuse.
- Containers are automatically removed after execution.
- Errors during container cleanup are logged but do not affect the execution result.
- Stdin is only attached if provided.
- Docker logs are parsed to separate stdout and stderr.

### backend/src/types/index.ts

#### Overview

# backend/src/types/index.ts

#### Module Overview

This file defines TypeScript types and interfaces used throughout the backend application. It provides a structured way to represent various entities and requests, ensuring consistency and type safety across the codebase.

#### Dependencies

| Import | Purpose |
|--------|---------|
| `Express` | Provides types for Express.js requests and responses. |

#### Classes

No classes are defined in this file.

#### Functions

No functions are defined in this file.

#### Interfaces

| Interface | Purpose |
|-----------|---------|
| `SupportedLanguage` | Represents the programming languages supported by the application. |
| `User` | Represents a user in the system. |
| `ExecutionOutput` | Represents the output of a code execution. |
| `Project` | Represents a project created by a user. |
| `UserStats` | Represents the statistics of a user. |
| `Achievement` | Represents an achievement unlocked by a user. |
| `AIAction` | Represents the types of actions the AI can perform. |
| `CodeContext` | Represents the context of a code snippet. |
| `CodeSuggestion` | Represents a suggestion for code improvement. |
| `ExecuteCodeRequest` | Represents a request to execute a piece of code. |
| `AIServiceRequest` | Represents a request to the AI service. |
| `AuthRequest` | Represents an authenticated request with user information. |

#### Notes

- The `timestamp` field in `ExecutionOutput` and `Project` interfaces is a Unix timestamp in milliseconds.
- The `executionsByLanguage` field in `UserStats` is a record that maps each supported language to the number of times the user has executed code in that language.
- The `lineRange` field in `CodeSuggestion` is optional and represents the range of lines in the code where the suggestion applies.

### backend/src/utils/auth.ts

#### Overview

# backend/src/utils/auth.ts

#### Module Overview

This file provides utility functions for handling authentication-related tasks, including password hashing, password comparison, JWT token generation, and validation of passwords and emails.

#### Dependencies

| Import | Purpose |
| ------ | ------- |
| `bcrypt` | Library for hashing and comparing passwords. |
| `jwt` | Library for generating and verifying JSON Web Tokens. |

#### Functions

| Function | Parameters | Returns | Description |
| -------- | ---------- | ------- | ----------- |
| `hashPassword` | `password: string` | `Promise<string>` | Hashes a plain text password using bcrypt. |
| `comparePassword` | `password: string`, `hash: string` | `Promise<boolean>` | Compares a plain text password with a hashed password. |
| `generateToken` | `userId: string`, `username: string` | `string` | Generates a JWT token for a user. |
| `verifyToken` | `token: string` | `{ id: string; username: string }` | Verifies a JWT token and returns the user's ID and username. |
| `validatePassword` | `password: string` | `{ valid: boolean; errors: string[] }` | Validates a password against a set of rules and returns errors if any. |
| `validateEmail` | `email: string` | `boolean` | Validates if a string is a well-formed email address. |

#### Configuration

| Variable | Default Value | Purpose |
| -------- | ------------- | ------- |
| `SALT_ROUNDS` | `10` | Number of salt rounds for bcrypt. |
| `JWT_SECRET` | `process.env.JWT_SECRET || 'your-secret-key-change-in-production'` | Secret key for signing JWTs. |
| `JWT_EXPIRES_IN` | `process.env.JWT_EXPIRES_IN || '24h'` | Expiration time for JWTs. |

#### Notes

- Ensure `JWT_SECRET` is set in the environment variables for production use.
- The default `JWT_SECRET` is a placeholder and should be replaced with a secure secret key.
- The `validatePassword` function returns an array of errors if the password is invalid.
- The `validateEmail` function uses a simple regex for email validation; consider using a more robust validation method if needed.

### backend/src/utils/gamification.ts

#### Overview

# Backend Utilities: Gamification

This file defines utility functions for calculating experience points (XP) and leveling up in our gamification system. It provides the core logic for awarding XP based on code length and success, and determining player levels and the XP required for the next level.

#### Dependencies

- **None**: This file does not import any external modules.

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `calculateXP` | `codeLength: number`, `success: boolean` | `number` | Calculates XP based on code length and success. |
| `calculateLevel` | `totalXP: number` | `number` | Calculates the player's level based on total XP. |
| `getXPForNextLevel` | `currentLevel: number` | `number` | Returns the XP required to reach the next level. |

#### Notes

- `calculateXP` multiplies a base XP value by a complexity multiplier and a success multiplier, ensuring XP does not exceed 1000.
- `calculateLevel` uses the square root of the total XP divided by 100 to determine the level.
- `getXPForNextLevel` calculates the XP needed for the next level by squaring the current level and multiplying by 100.

### backend/src/utils/logger.ts

#### Overview

# backend/src/utils/logger.ts

This file defines a logger utility using the `winston` library to handle logging in the application. It provides a centralized way to log messages to both console and file, with different formats for development and production environments.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `winston` | Logging library that provides flexible logging capabilities. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `winston.Logger` | Centralized logger instance | `createLogger`, `add`, `transports` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `createLogger` | `options` | `Logger` | Creates a new logger instance with specified options. |

#### Configuration

| Variable | Purpose |
| --- | --- |
| `logFormat` | JSON format for log files. Includes timestamp, error stack, and message. |
| `consoleFormat` | Colorized and timestamped format for console logs. Includes message and optional metadata. |
| `process.env.LOG_LEVEL` | Environment variable to set the log level (defaults to 'info'). |
| `process.env.NODE_ENV` | Environment variable to determine if the application is in production or development. |

#### Notes

- The logger uses `winston` to format and transport logs to both file and console.
- In non-production environments, console logs are colorized and include metadata.
- Log files are stored in the `logs` directory with `error.log` for errors and `combined.log` for all logs.
- The log level can be overridden via the `LOG_LEVEL` environment variable.

### frontend/.dockerignore

#### Overview

# frontend/.dockerignore

This file specifies files and directories to ignore when building Docker images for the frontend. It prevents unnecessary files from being included in the Docker context, which helps to reduce image size and speed up the build process.

#### Dependencies

- **node_modules**: Contains the dependencies required for the project.
- **dist**: Build output directory for compiled files.
- ***.env* files**: Environment configuration files that contain sensitive information.
- ***.log* files**: Log files that are generated during development or runtime.
- **.git**: Contains the entire Git repository, including commit history.
- **.gitignore**: Specifies intentionally untracked files to ignore.
- **README.md**: Project documentation that is not needed in the Docker build context.

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| N/A | N/A | N/A | N/A |

#### Configuration or Constants

- No configuration or constants are defined in this file.

#### Notes

- Ensure that any new files or directories that should not be included in the Docker build are added to this file.
- Be cautious when adding patterns to this file; incorrect patterns can inadvertently exclude necessary files from the build context.

### frontend/.env.example

#### Overview

# frontend/.env.example

This file contains environment variable definitions used to configure the frontend application. It serves as a template for developers to create their own `.env` file with the necessary environment-specific variables.

#### Dependencies

No direct dependencies are listed here, but it relies on the `dotenv` package to load environment variables from this file into the application.

#### Configuration

| Variable | Purpose |
|----------|---------|
| `VITE_API_BASE_URL` | Base URL for the API endpoints. |

#### Notes

- Developers should create a `.env` file from this example and populate it with the correct environment-specific values.
- The `VITE_` prefix is required for Vite to automatically load these variables.
- Ensure the `.env` file is added to `.gitignore` to avoid committing sensitive information to version control.

### frontend/Dockerfile

#### Overview

# frontend/Dockerfile

This file defines a Docker image for the frontend application, setting up the environment and dependencies needed to run the development server.

#### Dependencies

No direct dependencies are listed here, but it relies on the `node:20-alpine` base image which includes Node.js.

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| N/A | N/A | N/A | N/A |

#### Configuration

| Setting | Purpose |
| --- | --- |
| `FROM node:20-alpine` | Uses the Node.js 20 Alpine image. |
| `WORKDIR /app` | Sets the working directory to `/app`. |
| `COPY package*.json./` | Copies `package.json` and `package-lock.json` to the working directory. |
| `RUN npm install` | Installs dependencies listed in `package.json`. |
| `COPY. .` | Copies the entire application source code into the working directory. |
| `EXPOSE 5173` | Exposes port 5173 for the application. |
| `CMD ["npm", "run", "dev", "--", "--host"]` | Starts the development server using npm with the `dev` script and binds it to all network interfaces. |

#### Notes

- The development server runs on port 5173.
- Ensure Docker is installed and running on your machine to build and run this image.
- The `--host` flag binds the server to all network interfaces, which is useful for local testing but should be used with caution in production environments.

### frontend/index.html

#### Overview

# frontend/index.html

This file serves as the entry point for the frontend application. It sets up the basic HTML structure, includes the necessary meta tags, and links the main application script.

#### Dependencies

- **Vite SVG Icon**: The favicon is sourced from `/vite.svg`.

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| None | No classes are defined in this file. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| None | None | None | No functions are defined in this file. |

#### Configuration

- **Charset**: UTF-8
- **Viewport**: width=device-width, initial-scale=1.0

#### Notes

- The `<div id="root"></div>` element is where the React application will be mounted.
- The main application logic is loaded via `<script type="module" src="/src/main.tsx"></script>`.

### frontend/package.json

#### Overview

# frontend/package.json

#### Module Overview

This file defines the configuration for the frontend project, including scripts, dependencies, and development tools. It's crucial for setting up the project environment and managing its dependencies.

#### Dependencies

| Package | Purpose |
| --- | --- |
| `react` | Core library for building user interfaces. |
| `react-dom` | Necessary for rendering React components to the DOM. |
| `react-router-dom` | Provides routing for React applications. |
| `@monaco-editor/react` | Integrates Monaco Editor into React applications. |
| `axios` | Promise-based HTTP client for making API requests. |
| `react-toastify` | Library for displaying toast notifications. |

#### Scripts

| Script | Purpose |
| --- | --- |
| `dev` | Starts the development server using Vite. |
| `build` | Compiles TypeScript and builds the project using Vite. |
| `preview` | Serves the built project for preview. |
| `lint` | Runs ESLint to check for linting errors in TypeScript files. |

#### Notes

- The `dev` script is used for local development and hot-reloading.
- The `build` script compiles TypeScript and then builds the project for production.
- The `lint` script enforces strict linting rules to maintain code quality.

### frontend/postcss.config.js

#### Overview

# frontend/postcss.config.js

This file configures PostCSS plugins for our frontend project. It includes essential tools like TailwindCSS and Autoprefixer to streamline our CSS processing.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `tailwindcss` | Integrates TailwindCSS for utility-first CSS framework. |
| `autoprefixer` | Adds vendor prefixes to CSS rules using values from `Can I Use`. |

#### Configuration

| Configuration | Purpose |
| --- | --- |
| `plugins` | Object containing PostCSS plugins. |

#### Notes

- Ensure that TailwindCSS and Autoprefixer are installed as dependencies in `package.json`.
- This configuration is minimal but can be expanded with additional plugins as needed.

### frontend/tailwind.config.js

#### Overview

# frontend/tailwind.config.js

#### Module Overview

This file configures Tailwind CSS for the project. It specifies which files to process, the custom color palette, and any additional plugins.

#### Dependencies

- `import('tailwindcss').Config`: This import type is used to type-check the configuration object.

#### Configuration

| Key | Purpose |
| --- | ------- |
| `content` | Specifies the files that Tailwind should scan for class names. |
| `theme.extend.colors` | Extends the default color palette with custom colors for primary and dark themes. |

#### Functions

| Function | Parameters | Returns | Description |
| -------- | ---------- | ------- | ----------- |
| `extend` | `colors` object | None | Allows extending the default Tailwind color palette. |

#### Notes

- The `content` array includes all `.js`, `.ts`, `.jsx`, and `.tsx` files in the `src` directory, ensuring that Tailwind processes all relevant files.
- Custom color shades for primary and dark themes are defined under `theme.extend.colors`.
- The `plugins` array is currently empty, indicating no additional Tailwind plugins are used at this time.

### frontend/tsconfig.json

#### Overview

# frontend/tsconfig.json

This file configures the TypeScript compiler for the frontend part of the application. It sets compiler options that dictate how TypeScript code is compiled and ensures compatibility with modern JavaScript standards and React's JSX syntax.

#### Dependencies

- **TypeScript**: The primary dependency, used to compile TypeScript code to JavaScript.

#### Configuration

| Setting | Purpose | Details |
|---------|---------|---------|
| `target` | JavaScript version | ES2020 |
| `useDefineForClassFields` | Class field initialization | true |
| `lib` | Library files included | ["ES2020", "DOM", "DOM.Iterable"] |
| `module` | Module system | ESNext |
| `skipLibCheck` | Skip type checking of all declaration files | true |
| `moduleResolution` | Module resolution strategy | bundler |
| `allowImportingTsExtensions` | Allow importing.d.ts files | true |
| `resolveJsonModule` | Enable importing.json files | true |
| `isolatedModules` | Treat each file as a module | true |
| `noEmit` | Do not emit output files | true |
| `jsx` | JSX factory | react-jsx |
| `strict` | Enable all strict type-checking options | true |
| `noUnusedLocals` | Report errors on unused locals | true |
| `noUnusedParameters` | Report errors on unused parameters | true |
| `noFallthroughCasesInSwitch` | Report errors for fallthrough cases in switch statement | true |

#### Notes

- The `noEmit` option is set to `true` to allow the TypeScript compiler to work with the bundler without generating output files.
- The `strict` option ensures that type-checking is performed rigorously, helping catch potential issues early.
- The `jsx` option is set to `react-jsx` to enable JSX transformation for React components.
- The `include` section specifies that all TypeScript files in the `src` directory should be included in the compilation process.
- The `references` section includes settings from `tsconfig.node.json`, which may contain additional configurations for Node.js-specific settings.

### frontend/tsconfig.node.json

#### Overview

# frontend/tsconfig.node.json

This file configures TypeScript compilation for the Node environment in our frontend codebase. It ensures that TypeScript files are compiled correctly when running in a Node context, enabling seamless integration with our build tools.

#### Dependencies

No direct dependencies are listed here, but it relies on the TypeScript compiler (`typescript`) and the build tool configuration in `vite.config.ts`.

#### Configuration

| Setting | Purpose | Details |
| --- | --- | --- |
| `composite` | Enables incremental compilation | Speeds up build times by only compiling changed files |
| `skipLibCheck` | Skips type checking of all declaration files | Reduces compilation time by skipping library checks |
| `module` | Specifies the module code generation | Uses the latest ESNext module syntax |
| `moduleResolution` | Specifies module resolution strategy | Uses the bundler for resolving modules |
| `allowSyntheticDefaultImports` | Allows default imports from modules without `default` export | Enables compatibility with certain bundlers |

#### Notes

- This configuration is specifically tailored for the Node environment, ensuring that TypeScript files are compiled with Node-specific settings.
- The `include` section ensures that the `vite.config.ts` file is included in the compilation process, allowing for proper integration with Vite build configurations.

### frontend/vite.config.ts

#### Overview

# frontend/vite.config.ts

This file configures the Vite build tool for our React frontend application. It sets up plugins and server settings, including a proxy for API requests.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `defineConfig` | Function from `vite` to define the configuration object. |
| `react` | Plugin from `@vitejs/plugin-react` to enable React support in Vite. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `defineConfig` | None | Configuration object | Function to create a Vite configuration object. |

#### Configuration

| Setting | Value | Purpose |
| --- | --- | --- |
| `plugins` | `[react()]` | Enables React support. |
| `server.port` | `5173` | Sets the development server port. |
| `server.proxy` | `{ '/api': { target: 'http://localhost:3000', changeOrigin: true } }` | Proxy API requests to `http://localhost:3000`. |

#### Notes

- The `server.proxy` configuration is crucial for development as it allows us to forward API requests to the backend server running on `http://localhost:3000`.
- Ensure that the backend server is running before starting the frontend development server to avoid proxy errors.

### frontend/src/App.tsx

#### Overview

# frontend/src/App.tsx

#### Module Overview

This file defines the main application component, `App`, which sets up the routing and provides context for the editor functionality. It ensures that the application navigates to the editor page on the root path and redirects to the same page for any unmatched routes.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Router`, `Routes`, `Route`, `Navigate` | From `react-router-dom`, these components manage the routing in the application. |
| `ToastContainer` | From `react-toastify`, this component provides toast notifications. |
| `EditorProvider` | Custom context provider for editor-related context. |
| `Editor` | The main page component for the editor. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `EditorProvider` | Provides context for editor-related state and actions. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `App` | None | JSX elements | Main application component that sets up routing and context provider. |

#### Configuration

| Component | Configuration |
| --- | --- |
| `ToastContainer` | Positioned at the top-right, auto-closes after 3 seconds, does not hide the progress bar, displays newest toast on top, draggable, pauses on hover. |

#### Notes

- The `EditorProvider` wraps the entire application to provide context for the editor.
- Any unmatched routes will redirect to the root path (`/`).
- Toast notifications are styled in dark theme and appear at the top-right corner.

### frontend/src/index.css

#### Overview

# frontend/src/index.css

This file contains the primary CSS for the frontend application, leveraging Tailwind CSS for utility-first styling.

#### Dependencies

- `@tailwind base;`
  - Initializes base styles.
- `@tailwind components;`
  - Includes component styles.
- `@tailwind utilities;`
  - Provides utility classes.

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| N/A | N/A | N/A | N/A |

#### Configuration

- No configuration settings are defined in this file.

#### Notes

- The `body` styling ensures a consistent font across different browsers.
- The `#root` selector sets the root element to take up the full viewport height and width.

### frontend/src/main.tsx

#### Overview

# frontend/src/main.tsx

This file initializes the React application. It mounts the main `App` component to the DOM and applies global styles from `index.css`.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `React` | Core library for building the user interface. |
| `ReactDOM` | Provides the DOM-specific methods that React needs. |
| `App` | The main component that represents the entire application. |
| `index.css` | Global styles for the application. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| None | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `ReactDOM.createRoot` | `element` | `Root` | Returns a root object that can be used to render a React tree. |
| `ReactDOM.render` | `element`, `container` | `ReactComponent` | Renders a React element into a DOM container. |

#### Notes

- The `React.StrictMode` wrapper is used to highlight potential problems in the code.
- Ensure that the DOM element with the id `root` exists before running this file, as it will throw an error otherwise.
- This file is the entry point for the React application and should not be modified without understanding the implications on the entire application structure.

### frontend/src/components/AIAssistPanel.tsx

#### Overview

# AIAssistPanel.tsx

#### Module Overview

This file defines the `AIAssistPanel` component, a UI panel that allows users to interact with an AI assistant to get help with their code. It provides functionalities like explaining code, debugging errors, optimizing code, and converting between programming languages.

#### Dependencies

| Import | Description |
| --- | --- |
| `useState` | React hook for managing component state. |
| `useEditor` | Custom hook to access and modify the editor's state. |
| `aiAPI` | API service to communicate with the AI backend. |
| `toast` | Function from `react-toastify` to show notifications. |
| `AIAction` | Type definition for AI actions. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `handleSubmit` | `e: React.FormEvent` | `void` | Handles form submission to get AI assistance. |
| `applySuggestion` | `suggestion: string` | `void` | Applies a code suggestion to the editor. |

#### Notes

- The `handleSubmit` function checks if the query is empty and shows a warning toast if it is.
- The `applySuggestion` function updates the editor's code with the provided suggestion and shows a success toast.
- The component uses Tailwind CSS classes for styling.
- Ensure the `aiAPI` service is properly configured to communicate with the backend.
- The `AIAction` type should be defined in the `../types` directory and include all possible actions the AI can perform.

### frontend/src/components/CodeEditor.tsx

#### Overview

# frontend/src/components/CodeEditor.tsx

#### Module Overview

This file defines a React component `CodeEditor` that integrates Monaco Editor to provide a code editing interface. It allows users to write and edit code in various programming languages with syntax highlighting and other features.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Editor` | Component from `@monaco-editor/react` to render the Monaco Editor. |
| `useEditor` | Custom hook from `../contexts/EditorContext` to access editor state. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `CodeEditor` | Main component for the code editor. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `CodeEditor` | N/A | JSX element | Renders Monaco Editor with provided code and language settings. |

#### Configuration

| Configuration | Value |
| --- | --- |
| `languageMap` | Mapping of language names to Monaco Editor language identifiers. |

#### Notes

- The `Editor` component uses the `language` prop to determine the syntax highlighting.
- The `setCode` function is used to update the code state whenever the editor content changes.
- The `theme` is set to `vs-dark` for a dark-themed editor.
- The `options` object configures various editor settings like font size, minimap, tab size, and line numbers.

### frontend/src/components/Header.tsx

#### Overview

# frontend/src/components/Header.tsx

#### Module Overview

This file defines the `Header` component, which is the top navigation bar of the application. It includes the application title, buttons for toggling different features, and user statistics if available.

#### Dependencies

| Import | Purpose |
| ------ | ------- |
| `useState`, `useEffect` from 'react' | React hooks for state management and side effects |
| `useEditor` from `../contexts/EditorContext` | Context hook to access editor-related state and functions |
| `userAPI` from `../services/api` | API service for fetching user-related data |
| `UserStats` from `../types` | Type definition for user statistics |

#### Functions

| Function | Parameters | Returns | Description |
| -------- | ---------- | ------- | ----------- |
| `Header` | `onToggleAI: () => void`, `onToggleProjects: () => void` | JSX Element | Renders the header component with navigation and user stats |

#### Notes

- The `loadStats` function attempts to fetch user statistics when the component mounts. If the user is not authenticated, it logs a message and continues without error.
- The user statistics display is conditional, only showing if `stats` is not `null`.
- The progress bar width is dynamically calculated based on the user's XP percentage.

### frontend/src/components/LanguageSelector.tsx

#### Overview

# frontend/src/components/LanguageSelector.tsx

#### Module Overview

This file defines a `LanguageSelector` component that allows users to select a programming language from a dropdown menu. The selected language is managed via the `EditorContext`.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `useEditor` | Hook to access and modify the current language in `EditorContext`. |
| `SupportedLanguage` | Type definition for the supported languages. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `LanguageSelector` | Component to select a programming language. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `LanguageSelector` | N/A | JSX element | Renders a dropdown to select a programming language. |

#### Configuration or Constants

| Identifier | Type | Description |
| --- | --- | --- |
| `languages` | Array of objects | List of supported languages with their labels and icons. |

#### Notes

- The `select` element's `value` is controlled by the current language from `EditorContext`.
- The `onChange` handler updates the language in `EditorContext` when a new option is selected.
- Icons are used to visually distinguish languages in the dropdown.

### frontend/src/components/OutputConsole.tsx

#### Overview

# OutputConsole.tsx

This file defines the `OutputConsole` component, which displays the output of executed code within the application. It shows standard output, standard error, exit code, execution time, and memory usage.

#### Dependencies

| Import | Description |
| --- | --- |
| `useEditor` | Custom hook from `EditorContext` to access the editor's state. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `OutputConsole` | None | JSX Element | Renders the output console UI based on the editor's state. |

#### Notes

- The component relies on the `EditorContext` to get the current state of the editor, including the output and whether the code is currently executing.
- The UI dynamically changes based on the execution state, displaying a spinner when code is running and the actual output when the execution is complete.
- Ensure that the `EditorContext` is properly set up and provides the necessary state for this component to function correctly.

### frontend/src/components/ProjectsPanel.tsx

#### Overview

# ProjectsPanel.tsx

#### Module Overview

This file defines the `ProjectsPanel` component, which is a UI panel for managing projects. It allows users to view, save, load, and delete projects.

#### Dependencies

| Import | Description |
|--------|-------------|
| `useState`, `useEffect` | React hooks for managing state and side effects. |
| `useEditor` | Context hook to access and modify the editor's state. |
| `projectsAPI` | API service for interacting with project-related endpoints. |
| `toast` | Function from `react-toastify` for displaying notifications. |
| `Project` | Type definition for a project. |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `handleSaveProject` | `e: React.FormEvent` | `void` | Handles the submission of a new project save form. |
| `handleLoadProject` | `project: Project` | `void` | Loads a project into the editor. |
| `handleDeleteProject` | `id: string` | `void` | Deletes a project and refreshes the project list. |
| `loadProjects` | `void` | `void` | Fetches the list of projects from the API. |

#### Notes

- The `loadProjects` function is called once when the component mounts to initialize the project list.
- The `handleSaveProject` function validates the project name before attempting to save the project.
- The `handleDeleteProject` function prompts the user for confirmation before deleting a project.
- The `showSaveModal` state controls the visibility of the save project modal.

### frontend/src/components/ProtectedRoute.tsx

#### Overview

# frontend/src/components/ProtectedRoute.tsx

#### Module Overview

This file defines a `ProtectedRoute` component that restricts access to certain routes based on the user's authentication status. It uses the `AuthContext` to determine if the user is logged in and conditionally renders either the protected content or a loading indicator, or redirects to the login page if not authenticated.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Navigate` | From `react-router-dom`, used for redirecting unauthenticated users to the login page. |
| `useAuth` | Custom hook from `AuthContext` that provides authentication status and loading state. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `ProtectedRoute` | Component to protect routes based on authentication status. | None |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `ProtectedRoute` | `{ children: React.ReactNode }` | `JSX.Element` | Renders children if authenticated, loading indicator if loading, or redirects to login if not authenticated. |

#### Notes

- The `ProtectedRoute` component checks the authentication status using `useAuth`.
- If `isLoading` is true, it displays a loading indicator.
- If `isAuthenticated` is false, it redirects to the login page using `Navigate`.
- The component ensures that only authenticated users can access the routed content.

### frontend/src/contexts/AuthContext.tsx

#### Overview

# AuthContext.tsx

#### Module Overview

This file defines the `AuthContext` and `AuthProvider` for managing user authentication state across the application. It provides methods for logging in, registering, and logging out, and maintains the user's authentication status and token.

#### Dependencies

| Import | Description |
| :----- | :---------- |
| `createContext`, `useContext`, `useState`, `useEffect`, `ReactNode` | React hooks and context for state management and component composition. |
| `authAPI` | API service for authentication-related requests. |
| `User`, `LoginRequest`, `RegisterRequest` | Type definitions for user data and authentication requests. |
| `toast` | Utility for displaying notifications. |

#### Classes

| Class | Purpose | Key Methods |
| :---- | :------ | :---------- |
| `AuthContext` | Provides authentication context to the app. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| :------- | :--------- | :------ | :---------- |
| `AuthProvider` | `{ children: ReactNode }` | `ReactNode` | Provides authentication context to child components. |
| `login` | `data: LoginRequest` | `Promise<void>` | Authenticates a user with the provided login data. |
| `register` | `data: RegisterRequest` | `Promise<void>` | Registers a new user with the provided registration data. |
| `logout` | `void` | `void` | Logs the current user out and clears authentication data. |
| `useAuth` | `void` | `AuthContextType` | Custom hook to access the authentication context. |

#### Notes

- The `AuthProvider` component must wrap the application's component tree to provide authentication context.
- The `useAuth` hook must be used within the `AuthProvider` to access authentication methods and state.
- Error handling in `login` and `register` functions uses `toast` for user feedback.
- Authentication state is persisted in `localStorage` for session continuity.

### frontend/src/contexts/EditorContext.tsx

#### Overview

# EditorContext.tsx

This file defines the `EditorContext` and `EditorProvider` for managing the state of the editor within the application. It provides a way to share editor-related state across the app, such as the current code, language, execution output, and project.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `createContext`, `useContext`, `useState`, `useEffect`, `ReactNode` from 'react' | React hooks and context for state management and context provider |
| `SupportedLanguage`, `ExecutionResult`, `Project` from '../types' | Types for the supported languages, execution results, and projects |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `EditorContext` | Context for the editor state | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `EditorProvider` | `{ children: ReactNode }` | `ReactNode` | Provides editor context to its children |
| `useEditor` | N/A | `EditorContextType` | Custom hook to access the editor context |

#### Configuration

- The editor's code and language are persisted in `localStorage` to maintain state across sessions.

#### Notes

- Ensure that `useEditor` is used within an `EditorProvider` to avoid runtime errors.
- The default language is set to `'python'` if no saved language is found in `localStorage`.
- The `EditorContext` is initialized with `undefined` and will throw an error if accessed outside of an `EditorProvider`.

